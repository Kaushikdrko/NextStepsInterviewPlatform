# Base URL of the FastAPI backend (yns-api).
# Leave unset to run the dashboard on built-in mock data.
# When the backend is running, set this and switch lib/api/champion.ts to the
# apiGet/apiPost calls (examples are in that file).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
