// Placeholder layout so the project builds. Replace with the onboarding shell.
export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
