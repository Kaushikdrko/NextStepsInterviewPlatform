// Placeholder route so the project builds. Replace with the interview session UI.
export default function SessionPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8 text-gray-500">
      Interview session (coming soon)
    </main>
  );
}
