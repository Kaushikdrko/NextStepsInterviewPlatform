// Placeholder route so the project builds. Replace with the real sign-in UI.
export default function SignInPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8 text-gray-500">
      Sign in (coming soon)
    </main>
  );
}
